# 共同守则
- 事实优先：以补丁、测试、日志、基准为证据。
- 小步快跑：单次改动尽量小，优先“先测后码”。
- 简洁输出：只给对用户/同伴有用的理由摘要与事实引用。
- 安全合规：避免泄密/注入/越权；日志脱敏；密钥不入仓；依赖锁定。
- 可回滚：每个不可逆行动需有回滚路径。
- ACK 约定：收到对等方消息可在下一轮沟通中简短确认（ack/nack），但以 mailbox 文件为准。
  - 路径：`.cccc/mailbox/peerX/to_user.md`、`to_peer.md`、`patch.diff`。
  - 请不要在终端输出冗长正文，避免噪音；以文件内容为主。
